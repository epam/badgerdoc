name: Lint
on:
  pull_request:
    paths:
      - "**.py"

env:
  PYTHON_VERSION: 3.8
  LINT_CONFIG: pyproject.toml


jobs:
  lint:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        tool: [ black, isort ]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup python $PYTHON_VERSION
        uses: actions/setup-python@v5
        with:
          python-version: $PYTHON_VERSION
      - name: Get changed files
        id: changed-files
        uses: tj-actions/changed-files@v44
      - name: List changed files
        run: |
          echo Changed files: ${{ steps.changed-files.outputs.all_changed_files }}
      - name: Install python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install black==24.4.2 isort==5.13.2
      - name: Run Black
        if: ${{ matrix.tool }} == black
        run: |
          black --config $LINT_CONFIG --diff --check ${{ steps.changed-files.outputs.all_changed_files }}
      - name: Run Isort
        if: ${{ matrix.tool }} == isort
        run: |
          isort --config $LINT_CONFIG --check-only {{ steps.changed-files.outputs.all_changed_files }}
